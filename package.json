{
  "name": "printflow",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "deploy": "git pull origin main && npm install && npm run build --workspace=frontend && rm -rf /var/www/flowktp/* && cp -r frontend/dist/* /var/www/flowktp/ && pm2 restart printflow-api"
  }
}
